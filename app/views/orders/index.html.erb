<h1>Your Orders</h1>
<div class>
  <div class="card">
    <div class="card-body">
      <% if @orders.size == 0 %>
        <p>You do not have any existing orders, <%= link_to "make some", products_path %>!</p>
      <% else %>
        <% @orders.each do |order| %>
            <div class="card">
              <div class="card-body">
                <%= link_to(order, class: "text-dark text-decoration-none") do %>
                  <div>
                    <p class="mb-1">Order #: <%= order.stripe_session %></p>
                    <div class="row">
                      <div class="col-md">
                        <p class="card-text">Order Total: <%= number_to_currency(order.total.to_d / 100) %></p>
                      </div>
                      <div class="col-md">
                        <p class="card-text text-md-right">Order Date: <%= order.created_at.strftime("%b %d, %Y") %></p>
                      </div>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
